apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.env.database_name }}
  namespace: {{ .Values.namespace.name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.env.database_name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: {{ .Values.env.database_name }}
